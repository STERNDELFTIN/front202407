// 별자리를 묻고 같으면 alert를 띄워준다.
let sign = prompt("당신의 별자리는? ");

if(sign === "사자자리"){
    // 같은 사자자리일 때, 여기를 수행
    alert("저랑 같은 별자리네요!");
}
else{
    // 같은 사자자리가 아닐 때, 여기를 수행
    alert("안타깝네요~ 저랑 다른 별자리네요!");
}